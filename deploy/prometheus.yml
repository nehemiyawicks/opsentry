global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: opsentry
    metrics_path: /metrics
    static_configs:
      - targets:
          - opsentry:8080
        labels:
          instance: opsentry
